# Docker Compose for running all test levels.
#
# Uses profiles to start only the services needed for each test type:
#
#   Unit tests (no infra):
#     docker compose -f docker-compose.test.yml run --rm unit-tests
#
#   Integration tests (Redis, Quickwit, PostgreSQL):
#     docker compose -f docker-compose.test.yml --profile integration up -d --wait
#     docker compose -f docker-compose.test.yml run --rm integration-tests
#     docker compose -f docker-compose.test.yml --profile integration down
#
#   E2E tests (full stack from docker-compose.yml must be running):
#     docker compose up --build -d
#     docker compose -f docker-compose.test.yml run --rm e2e-tests
#     docker compose down

services:

  # ── Test runners ──────────────────────────────────────────────────────────

  unit-tests:
    build:
      context: .
      dockerfile: Dockerfile.test
    profiles: ["unit"]
    command: ["-v", "--tb=short", "-m", "not integration and not e2e", "tests/"]

  integration-tests:
    build:
      context: .
      dockerfile: Dockerfile.test
    profiles: ["integration"]
    command: ["-v", "--tb=short", "-m", "integration", "tests/"]
    environment:
      REDIS_URL: redis://redis:6379/0
      QUICKWIT_URL: http://quickwit:7280
      DB_DSN: postgresql://chris:chris1234@postgres:5432/chris_streaming_test
    depends_on:
      redis:
        condition: service_healthy
      quickwit:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - test

  e2e-tests:
    build:
      context: .
      dockerfile: Dockerfile.test
    profiles: ["e2e"]
    command: ["-v", "--tb=long", "-m", "e2e", "-s", "tests/"]
    environment:
      SSE_SERVICE_URL: http://sse-service:8080
      PFCON_URL: http://pfcon:30005
      REDIS_URL: redis://redis:6379/0
      QUICKWIT_URL: http://quickwit:7280
      DB_DSN: postgresql://chris:chris1234@postgres:5432/chris_streaming
    networks:
      - streaming

  # ── Infrastructure (integration profile) ──────────────────────────────────

  redis:
    image: redis:7-alpine
    profiles: ["integration"]
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --appendfsync
      - everysec
    networks:
      - test
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 10

  quickwit:
    image: quickwit/quickwit:latest
    profiles: ["integration"]
    command: ["run"]
    environment:
      QW_METASTORE_URI: "file:///quickwit/qwdata/metastore"
      QW_DEFAULT_INDEX_ROOT_URI: "file:///quickwit/qwdata/indexes"
    networks:
      - test
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:7280/health/livez >/dev/null 2>&1 || curl -sf http://localhost:7280/health/livez || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

  postgres:
    image: postgres:16-alpine
    profiles: ["integration"]
    environment:
      POSTGRES_DB: chris_streaming_test
      POSTGRES_USER: chris
      POSTGRES_PASSWORD: chris1234
    networks:
      - test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chris -d chris_streaming_test"]
      interval: 3s
      timeout: 3s
      retries: 10

networks:
  test:
  streaming:
    external: true
    name: chris_streaming_workers_streaming
